import { memo } from 'react';

const MainPage = memo(() => {
    // eslint-disable-next-line i18next/no-literal-string
    return <div data-testid='MainPage'>MainPage</div>;
});

export default MainPage;
